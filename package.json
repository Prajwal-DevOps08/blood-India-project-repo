{
  "name": "test-pipeline-project",
  "version": "1.0.0",
  "description": "Project for Jenkins test pipeline",
  "main": "index.js",
  "scripts": {
    "build": "echo 'Building project...' && node index.js",
    "start": "node index.js",
    "test": "echo \"No tests configured, skipping...\" && exit 0"
  },
  "dependencies": {},
  "devDependencies": {}
}
